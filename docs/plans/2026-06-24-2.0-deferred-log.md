# NumPad 2.0 — Deferred / Skipped / Assumptions Log

Running list of things intentionally **not** done in this push, to review **before shipping 2.0**.
Started 2026-06-24. Companion to `2026-06-24-2.0-monetization-and-scope-design.md`.
Append here whenever we cut scope, defer, or assume.

---

## Deferred features (revisit before ship)
- **Phase 1 — on-device verification.** Customizable keyboard is built + 92 tests green, but
  drag-reorder / add-remove / live keyboard re-render are NOT device-verified. _[owner — needs phones]_
- **Phase 1 — follow-ups.** Advanced token dispatch (cursor/hide/tab/calc/snippet/pack/overlay
  currently render as `.blank` in `renderKind`); per-key colour/size/span + `keyScale`; label
  override (split `Item` display vs inserted value); layout export/import (JSON via ShareLink/fileImporter).
- **Currency conversion** (Units&Conversion pack) — needs live FX rates (network); `UnitConverter`
  is intentionally offline. Ship length/mass/temperature (+ any other static units) only.
- **International/Formatting pack** — full phone / IBAN / national-ID formatting is
  libphonenumber-class. Scope kept tight: locale decimal/grouping separators, percent, currency
  symbol, basic phone grouping. Full validation/parsing deferred.
- **Phase 3 — non-requested items.** Inline calculator is already implemented behind a flag;
  left as-is (owner did not request surfacing it in this push). Any other "supporting" ideas
  beyond the four requested (iCloud sync, cursor controls, smart pack defaults, result tape).

## Assumptions to confirm
- **Original premium packs (finance / symbols / programmer) sold à la carte at $1.99 each**
  (vs. Pro-only). Inferred; owner did not object when flagged. Trivial to flip to Pro-only.
- **Early-bird window is per-user 72h from first 2.0 launch** (not a fixed global calendar window).
- **`.custom` keys row + premium themes are Pro perks** (not à la carte $1.99 packs).

## Phase 2 — pack contents to review (built 2026-06-24)
First-pass key rows (10 keys each) — tune freely, trivial to change in `PackKeys` / `DateTimeTokens`:
- **Units & Conversion:** `cm m km in ft mi kg lb °C °F`
- **Scientific:** `π e √ ^ ² ³ × ÷ ± °`
- **Business:** `$ € £ ¥ ¢ % ‰ ( ) #`
- **Programmer+:** `0b != == && || => -> { } _`
- **International:** `€ £ ¥ ₹ ₩ – — … ° №` (static intl currency + punctuation)
- **Date & Time** (computed — inserts live values): Date, Time, D+T, Day (weekday), Mon, DD, YYYY, ISO, ISO+, Unix

Notes:
- International is intentionally a **static** symbol row. Dynamic locale **currency-symbol** and
  **phone/IBAN** formatting stay deferred (libphonenumber-class). Locale **decimal/grouping
  separators** remain an experimental (non-GA) option via `localeAwareSeparators` — NOT promoted in
  this pass (only the four explicitly-requested behaviors were).
- All 6 new packs are **Pro-gated for now**; à la carte $1.99 pricing + product IDs land in Phase 5.
- `SharedExtensions.swift` keeps growing (now also `PackKeys` + `DateTimeTokens`). Consider extracting
  pack engines into their own shared files (follow-up; kept inline for zero Xcode target-wiring).

## Phase 3 — GA promotion notes (2026-06-24)
- Promoted to GA (now `UserPrefs`, default **ON** for everyone; toggle in Store → Settings):
  **Inline Calculator, Cursor Controls, Smart Pack Defaulting, Result Tape.**
- ⚠️ **Inline Calculator was promoted although not in the explicit list** — the Result Tape is fed
  *only* by calculator results, so the tape is meaningless without it. Say if you'd rather keep the
  calculator off (then the tape should be cut too).
- Defaulting Inline Calculator ON changes the **"="** key to evaluate the trailing expression (it
  falls back to inserting a literal "=" when the text isn't a valid expression). Confirm that's desired.
- NOT promoted (still experimental, DEBUG/TestFlight-only): Conversion Overlay, Locale-Aware
  Separators, Save Snippet From Keyboard, Fast Delete. Easy to promote later if wanted.
- Dead code: removed the 4 promoted getters/stored props from `FeatureFlags`; the old `ff*` Constants
  keys for them are now unused (harmless) — clean up in a later pass.

## Phase 3 — iCloud sync notes (2026-06-24)
- Implemented as `CloudSync`: app-group ↔ `NSUbiquitousKeyValueStore` mirror, **Pro-gated** + user
  opt-in (Store → Settings → iCloud Sync; default OFF). Syncs snippets, custom pack/slots, custom
  layouts + active id, theme, height preset. Push on background; pull on foreground + external change.
- ⚠️ **Clipboard is intentionally EXCLUDED despite "option 2".** Clipboard history lives in the
  **keychain** (it can hold passwords / 2FA / card numbers); copying it into plaintext iCloud KVS
  would regress that security decision. Proper path = iCloud **Keychain** (`kSecAttrSynchronizable`),
  a separate effort — **DEFERRED**. Confirm you accept clipboard not syncing for now.
- ⚠️ **Owner-gated capability:** the iCloud **Key-Value storage** capability must be enabled for the
  app id in the Apple Developer portal / Xcode signing. The entitlement is declared in
  `NumPad.entitlements`, but device/archive builds need it provisioned. Until then, KVS calls no-op.
- ⚠️ **Owner-gated verification:** real cross-device sync needs two iCloud-signed devices — NOT verified.
- Conflict strategy is **whole-value last-writer-wins per key** (KVS default), not a semantic union
  merge — concurrent edits to a collection on two devices can drop the older side. Union-merge is a
  refinement — **DEFERRED**.

## Phase 5 — monetization notes (2026-06-24)
- New gating: paid **base** (default/math/math2 + free themes + free UX) + **9 à la carte $1.99 packs**
  + **Pro $11.99** (custom-keys pack, premium themes, customizable keyboard, iCloud — all Pro-only).
  `StoreManager` loads all products and tracks per-pack ownership; the legacy Finance bool is bridged.
- **Early-bird:** 72h discounted Pro (`numpad.pro.lifetime.earlybird`) for pre-2.0 users + two reminders
  (+1h, +66h), cancelled on Pro purchase.
- **First-run funnel repurposed:** the post-splash upsell now shows ONLY to early-bird-eligible users
  (the paid app no longer hard-upsells new buyers); after the 72h window there's no post-splash upsell.
- **Standalone $4.99 custom keyboard:** never sold (not in the catalog) → custom keyboard is Pro-only.
  The dormant `isCustomKeyboardPurchased` / standalone-entitlement code remains (harmless) — remove later.
- **`.storekit`:** `NumPad/Products.storekit` added (all 11 products + prices) for sim testing and as the
  ASC product spec. ⚠️ Owner step: Edit Scheme → Run → Options → StoreKit Configuration → select
  `Products.storekit` to exercise purchases in the simulator (not wired into the scheme in-repo).

## Risks / review-before-ship
- **Notification permission for a promo** — requesting auth solely to advertise the early-bird
  offer is borderline per App Review (push-for-marketing). Mitigation: contextual pre-prompt; the
  in-app countdown works without permission. Revisit copy + request timing.
- **Going paid kills the free funnel** — the 1.8.0 first-run upsell + context-aware paywall hero
  + purchase-funnel analytics become moot. We remove/repurpose them; confirm there's no funnel
  analytics we still want to keep.
- **Catalog size** — 9 pack SKUs + 2 Pro SKUs (full + earlybird) = 11 IAP products to
  create/price/maintain in ASC.

## Owner-gated (cannot be done in code)
- **ASC:** app → $2.99 tier; reprice `numpad.pro.lifetime` → $11.99; create 8 new pack products +
  `numpad.pro.lifetime.earlybird`; keep `numpad.pack.finance`; pricing + approval.
- **Device regression** on both phones (iOS 26.5 + 27.0).
- **App Store submission** — never auto-submitted; owner clicks Submit.

## UX overhaul — Phase 1 (packs 12→5) follow-ups (2026-06-24, branch feat/2.0-ux-overhaul)
Commits 51d2e8a · 09683a7 · 11e6601 · 0f2f445 · e7b2cc1 (full suite 124 green). Review before ship:
- **ASC catalog is now 6 IAPs, not 11** (supersedes the "create 8 new pack products" line above):
  keep `numpad.pack.{finance,symbols,programmer,datetime}` + `numpad.pro.lifetime` + `numpad.pro.lifetime.earlybird`.
  Do NOT create units/scientific/business/intl/programmerplus products. (Owner-gated ASC step.)
- **Visual:** the merged finance/symbols/programmer rows now render in `.text` font (was numeric). Sensible
  for multi-char glyphs (0x, <<, !=, ‰, ₹, √, ≈) but **owner should eyeball on device**. Note `.scientific`/`.business`
  rows (still decode-only) remain numeric font — pre-existing inconsistency, out of scope.
- **Localization:** only `en.lproj` was rekeyed `"Symbols"`→`"Symbols & Science"`. The other 15 locales still carry
  the dead `"Symbols"` key (they fall back to the English literal at runtime). The **key itself changed**, so the
  49-locale translation pass must remove/re-key the old `"Symbols"` entries, not just translate a value.
- **Doc drift:** `CLAUDE.md` Monetization section still describes the old 2-non-consumable model (`isLocked(pack:)`,
  finance-only IAP) — doesn't mention `isPackLocked`/the à-la-carte five. Run a doc-updater pass before 2.0 docs are final.
- **Custom pack activation:** `.custom` left the pack picker; until UX Phase 4 (Custom Keys redesign) ships, there is
  no in-app way to activate the build-your-own pack row. Intentional, sequential — not a bug.
- **Copy nit (cosmetic, left):** the Features & Guide "Math" row says "Free." while the other four rows state no price;
  drop it for parallel voice during a later copy pass.
